# Abstention policy
Missing artifacts, disabled corroboration, non-computable CSS, robots blocks, and failed evidence locators become `insufficient_evidence`, never findings. The runtime cap emits a partial truncated report.
